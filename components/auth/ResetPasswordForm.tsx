"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import styles from "./AuthForm.module.css";
export default function ResetPasswordForm({token}:{token:string}){const router=useRouter();const [error,setError]=useState("");const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const fd=new FormData(e.currentTarget);try{const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:fd.get("password"),confirmPassword:fd.get("confirmPassword")})});const d=await r.json();if(!r.ok)setError(d.error??"Паролата не беше сменена.");else setDone(true);}catch{setError("Няма връзка със сървъра.");}finally{setLoading(false)}}if(done)return <main className={styles.page}><section className={styles.card}><span className={styles.eyebrow}>ГОТОВО</span><h1 className={styles.title}>Паролата е сменена</h1><p className={styles.subtitle}>Вече можеш да влезеш с новата си парола.</p><button className={styles.button} onClick={()=>router.push("/login")}>Към вход</button></section></main>;return <main className={styles.page}><section className={styles.card}><span className={styles.eyebrow}>НОВА ПАРОЛА</span><h1 className={styles.title}>Смени паролата</h1><p className={styles.subtitle}>Използвай поне 8 знака, буква и цифра.</p><form className={styles.form} onSubmit={submit}><div className={styles.field}><label>Нова парола</label><input name="password" type="password" minLength={8} autoComplete="new-password" required/></div><div className={styles.field}><label>Повтори паролата</label><input name="confirmPassword" type="password" minLength={8} autoComplete="new-password" required/></div>{error&&<div className={styles.error}>{error}</div>}<button className={styles.button} disabled={loading}>{loading?"Запазване...":"Смени паролата"}</button></form><p className={styles.switch}><Link href="/login">Назад към вход</Link></p></section></main>}
